FROM python:3.10-slim

RUN apt-get update && apt-get install -y git
RUN python -m pip install --upgrade pip

WORKDIR /app
COPY requirements.astrology.txt .
RUN pip install --no-cache-dir -r requirements.astrology.txt

COPY src/ src/

CMD ["python", "src/mcp_tools/astrology/tools.py"]