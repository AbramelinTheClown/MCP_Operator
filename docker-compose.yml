version: '3.8'

services:
  nasa-images-mcp:
    build:
      context: .
      dockerfile: Dockerfile.nasa_images
    ports:
      - "3003:3003"
    env_file:
      - .env
    volumes:
      - ./src/mcp_tools/nasa_images:/app/src/mcp_tools/nasa_images
